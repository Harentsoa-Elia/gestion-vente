You are an expert at developping web application with Next.js(typescript), fastAPI and postgresql. I want to create a web application for managng concert's tickets. I want the user of this app to be able to create tickets in pdf format(In fact, I want a form in this app where the user can enter the title of the concert, the description, the price of the tickets, the code of the concert and a QR code). The title is the title of the concert, the description is the description of the concert(what is it about, etc).The code of the tickets is really important because it will be used to generate the ID of each tickets. For example, the code of the ticket for the concert of a group called MadGuys is MDG and the Id of the tickets generated by the app will be MDG1, MDG2, ..., MDG167, etc. That's the use of the code. For the QR code, primary in the database, the table tickets has an attribute called is_used(it is a boolean field and by default its value is false. It will be used for checking if the ticket was already used or not); when someone scans the QR code on the tickets, the person who scans the QR code(the security) will get a visual feedback showing OK you can pass or something like that when the value of the attribute is_used is false and set the value of is_used to be true after that. When someone scans the QR code on the tickets and the value of the attribute is_used is true(this is the case where the ticket is scanned multiple times), he will get a visual feedback showing that the ticket is not valid anymore because it was already used. Make the visual feedback easy to understand for example a green check when the ticket is valid and a red cross when the ticket was already used. I want a button save to save the configurations of the tickets which will be created, so the generator of the tickets can just specify how many tickets to generate in pdf format each time entering the app; next time for a new concert the user can just change the configurations, save it and generate tickets of the new concert. For generating the tickets id, I want the number concatenated after the code to be autoincrement. For structuring the backend codes with fastAPI,I want you to follow this structure:
│   main.py
│
└───app
    │   database.py
    │   __init__.py
    │
    ├───controllers
    ├───models
    ├───schemas
    └───services

alembic revision --autogenerate -m "create table result now"
alembic upgrade head

python -m venv venv
venv/Scripts/activate
pip install -r requirements.txt
uvicorn main:app --reload
http://localhost:8000/docs
uvicorn main:app --host 0.0.0.0 --port 8000 --reload
netsh advfirewall set allprofiles state off
netsh advfirewall set allprofiles state on
